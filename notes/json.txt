keys should be in double quotes(only strings allowed)

values :
strings -> double quotes
boolean, integer, array, objects -> no quotes

JSON.stringify(arr/object) -> converts json to strings so tha it can be trasferred 
JSON.parse(json) -> converts string to json so tha we can use it like a normal object like obj.prop